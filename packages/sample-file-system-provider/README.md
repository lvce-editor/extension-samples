# File System Provider Sample

Registers a writable `memfs` file system containing a welcome file and a source folder. The provider implements directory reads, file reads and writes, folder creation, deletion, and rename.
